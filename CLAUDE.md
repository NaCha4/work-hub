# CLAUDE.md

이 저장소의 작업 규칙은 [AGENTS.md](AGENTS.md) 에 있다. 작업 전에 반드시 읽는다.

특히 다음 세 가지는 예외 없이 지킨다.

1. **`git push` 는 어떤 경우에도 실행하지 않는다.**
2. 사용자가 초기 구성 완료를 직접 선언하기 전까지 **커밋하지 않는다.**
3. 세션 시작 시 원격에 받아올 것이 있으면 `git pull --ff-only` 로 먼저 반영한다.
